<div class="flex flex-col gap-2 p-2">
  <div>
      <label>Ime i prezime</label> {{user.name}}
      <input type="text" value="{{user.name}}"/>

</div>
<div class="pb-2 flex flex-col">
    <label>E-mail adresa:</label> {{user.email}}
      <input type="email" name="email" value="{{user.emil}}"/>
</div>
<div class="b-2 flex flex-col">
     <label >Zaporka</label> 
      <input type="password" name="password" />
 </div>
 <div class="pb-2 flex gap-2">
    <input type="submit" value="Potvrdi" class="flex-1 btn btn-primary">
    <input type="reset" value="Poništi" class="flex-1 btn btn-primary">
 </div>
</div>